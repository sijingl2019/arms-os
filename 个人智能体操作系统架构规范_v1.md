# 个人智能体操作系统（Agentic OS）架构规范 v1

> 基于 ARMS 框架（Applications / Routines / Memory / Skills）改写，核心修正：Skill 是"大脑"（流程与判断），Application/Connector 是"手"（触达外部世界的通道），两者是协作关系而非替代关系。本规范面向以 Claude Code（或 Codex 等同类 agent CLI）为执行引擎的个人自动化系统。

---

## 1. 设计目标与原则

**目标**：日常重复性工作沉淀为 Skill，通过对话、命令行、定时任务三种方式触发；系统能在无人值守的情况下安全地完成真实世界的操作；随着使用时间增长，系统越用越顺手，而不是越用越乱。

**五条原则**：

1. **重复即封装**——同一类任务被要求两次，就该变成一个 Skill。这是整个系统增长的唯一入口，不靠"预先设计好一套完整系统"。
2. **Skill 管流程，Connector 管触达**——Skill 文件里写的是"做什么、按什么顺序、遇到什么情况要停下来"；真正碰外部系统（发消息、读日历、操作专有软件）永远通过 Application 层的 CLI / API / MCP 完成，不在 Skill 里裸写平台专属的猜测性操作。
3. **文件即记忆，索引优于整理**——不追求人眼友好的文件夹层级，追求 agent 一跳到位的路由文件（router file）。
4. **默认只读，写操作要留痕**——无人值守执行时，删除、发送、下单、发布类操作必须在 Skill 里显式标注为"高风险动作"，要么需要人工确认，要么必须写运行日志。
5. **小步启动，逐层加固**——落地顺序自底向上：Skill → Memory → Routine → Application → 指挥中心。任何一层没做扎实，上一层加得越多越危险。

---

## 2. 分层架构总览

```
                     ┌─────────────────────────┐
                     │   指挥中心 (Command Centre) │   ← 你每天打开的唯一入口
                     │   展示状态，不存储真相       │
                     └───────────┬─────────────┘
                                 │
        ┌────────────┬──────────┼──────────┬────────────┐
        │            │          │          │            │
   ┌────▼───┐   ┌────▼───┐  ┌───▼────┐ ┌───▼─────┐  ┌───▼────┐
   │ Skill  │──▶│ Memory │  │Routine │ │Application│  │  You   │
   │(大脑：  │   │(记忆：  │  │(定时： │ │ (手：真实  │  │(人工   │
   │流程判断)│   │路由索引)│  │何时跑) │ │ 世界通道) │  │确认点) │
   └────────┘   └────────┘  └────────┘ └──────────┘  └────────┘
        │                                     ▲
        └─────── Skill 执行到"碰外部系统"这一步 ─┘
                 时，调用 Application 层的
                 CLI / API / MCP 工具
```

**调用关系**：Routine 到点触发 Skill → Skill 读 Memory 里的路由文件找到需要的上下文/历史产出 → Skill 执行到需要操作外部系统的步骤时，调用 Application 层暴露的工具 → 执行结果写回 Memory（新的文件/索引条目）→ 指挥中心下次打开时读到最新状态。

---

## 3. 目录结构规范

以 Claude Code 风格为例（`~/workspace/` 为工作区根目录）：

```
~/workspace/
├── CLAUDE.md                 # 主路由文件（master router）：我是谁、几大工作领域、
│                              # 每个领域指向哪个索引文件
├── .claude/
│   └── skills/                # 所有 Skill 存放处
│       ├── SKILLS_INDEX.md    # Skill 库自己的路由索引（见 4.3）
│       ├── knowledge-cleanup/
│       │   └── SKILL.md
│       ├── news-digest/
│       │   ├── SKILL.md
│       │   └── references/    # 富文件：模板、示例、清单
│       └── post-publisher/
│           └── SKILL.md
├── areas/
│   ├── CONTENT.md              # 内容领域索引
│   ├── CLIENTS.md
│   └── FINANCE.md
├── connectors/                 # 自建 CLI / 小工具（Application L3 产出物）
├── output/
│   └── artifacts/               # Skill 产出的文件、报告、草稿
├── runs.log                    # 所有 headless / routine 执行的运行记录
└── command-centre/
    └── index.html               # 指挥中心页面
```

---

## 4. Skill 层规范

### 4.1 SKILL.md 模板（控制在 60 行以内）

```markdown
---
name: news-digest
description: 每天早上抓取指定信源，生成一份精简资讯摘要
triggers:
  - "/news-digest"
  - "今天有什么新闻"
model_hint: claude-sonnet-5     # 日常任务用中档模型，省成本
effort_hint: medium
---

## 这个 Skill 做什么
1. 从 areas/CONTENT.md 里列出的信源清单抓取当日更新
2. 用统一格式（标题+一句话+链接）整理成摘要
3. 写入 output/artifacts/news/YYYY-MM-DD.md
4. 追加一行记录到 runs.log

## 绝对不能做的事（护栏）
- 不自动转发/发布到任何外部平台
- 不访问 areas/CONTENT.md 之外声明的信源
- 抓取失败的信源要在摘要里明确标注"未获取"，不要编造内容

## 需要人工确认的动作
（本 skill 无高风险动作，全程只读）

## 依赖的 Application
- fetch-cli（connectors/fetch-cli，见 Application 层）
```

**必备字段**：`triggers`（对话触发词 + 命令行触发名）、步骤、**绝对不能做的事**、**需要人工确认的动作**、依赖的 Application/Connector。后两项是本规范对原版 ARMS 的强制补充，防止无人值守时出现不可逆操作。

### 4.2 复杂 Skill 拆分为 Skill Tree

单文件超过 150 行，或明显混杂多个任务时，拆分为：

```
post-publisher/
├── SKILL.md              # 路由器：列出子任务对照表，不写具体步骤
├── weibo-post.md
├── xiaohongshu-post.md
└── templates/
    └── post-template.md
```

`SKILL.md` 只做路由，具体执行逻辑下沉到对应文件，agent 按需只读取匹配的那一个文件。

### 4.3 Skill 库索引（SKILLS_INDEX.md）

随着 Skill 数量增长（超过 15-20 个后明显需要），维护一份索引，防止触发词冲突、功能重复：

```markdown
| Skill 名 | 一句话用途 | 触发词 | 状态 | 是否含高风险动作 |
|---|---|---|---|---|
| news-digest | 每日资讯摘要 | /news-digest | 稳定 | 否 |
| post-publisher | 发布内容到社媒 | /publish | 稳定 | 是（发布不可撤回）|
```

新增 Skill 前，先查这份索引，避免触发词撞车或重复造轮子。

### 4.4 三种触发方式落地

- **对话触发**：agent 根据 `description` 和用户表述自动匹配，日常最常用。
- **命令行触发（headless）**：`claude -p "/news-digest" --model claude-sonnet-5`，一次性执行不开对话窗口，用于指挥中心按钮、内部工具调用。
- **定时触发**：见第 6 章 Routine 层，本质是把命令行触发挂到调度器上。

---

## 5. Memory 层规范

### 5.1 两级路由文件

- **主路由（`CLAUDE.md`）**：一句话自我定位 + 几大领域列表 + 每个领域对应的索引文件路径。
- **领域索引（`areas/*.md`）**：该领域的关键文件一行一个，不写文档式说明，只做指针。

**铁律**：文件挪动或新项目启动时，必须在同一轮操作里更新对应路由，"过期的指针比没有指针更糟"。

### 5.2 可视化 second brain（可选，进阶）

一个本地 HTML 页面，读取上述路由文件把工作区画成可缩放、可搜索的图谱。价值在于"给别人看"和"人眼扫描"，不是给 agent 用的（agent 用路由文件已经足够快）。优先级低于 5.1，量力而行。

---

## 6. Routine 层规范

### 6.1 调度方式选择

| 方案 | 适用场景 | 限制 |
|---|---|---|
| 本地定时任务（电脑需开机） | 轻量、非关键任务 | 关机时不跑，需明确"跳过"还是"开机补跑" |
| 云端常驻小机器（VPS）+ 文件同步（如 Syncthing） | 需要 7×24 可靠执行的任务 | 需要维护一台常驻机器、同步链路 |

> **重要**：本项目环境中的"定时任务"必须通过平台提供的持久化调度工具创建，**不要**使用会话内的临时 cron 机制——那类调度会随会话结束而失效，看起来配置成功、实际上永远不会触发，是最容易踩的坑。

### 6.2 可靠性三件套

1. **日志**：每次 Routine 触发的 Skill 执行完，向 `runs.log` 追加一行（时间、Skill 名、成功/失败、产出路径）。
2. **失败处理**：明确写清楚"失败要不要重试、重试几次、超过阈值要不要通知你"，不能默认静默失败。
3. **幂等性**：定时任务被重复触发（比如补跑）不应产生重复副作用（比如重复发布同一条内容）。

### 6.3 多机同步（如启用云端常驻）

只同步工作区数据文件夹，明确排除凭据、临时文件；同步工具的管理界面不对公网暴露，仅本地访问。

---

## 7. Application 层规范

### 7.1 通道优先级（从优到劣）

1. **官方 MCP Server** —— 有维护、有社区背书，优先选择
2. **官方 API** —— 灵活但需要自己管理密钥和调用逻辑
3. **自建 CLI / 脚本** —— 平台没有官方通道时的补位方案，本质是"临时造一个 Application"
4. **浏览器自动化 / 网页模拟操作** —— 最后手段，脆弱、易受平台反爬和条款限制，仅用于低频、非关键、且明确知晓平台允许的场景

**决策规则**：Skill 里凡是要"碰外部系统"的步骤，先查这个优先级表，不要一上来就写浏览器自动化脚本。

### 7.2 凭据管理

- 凭据只通过对应 App 自己的登录/授权流程获取，绝不让 agent 把凭据传给无关服务。
- 新接入的 connector 先以只读权限验证一次成功调用，再决定是否开放写权限。

### 7.3 安全边界

- 默认所有 Application 调用为只读；写操作/发布类操作，在对应 Skill 里必须列入"需要人工确认的动作"（见 4.1）。

---

## 8. 指挥中心（Command Centre）

**定位**：一个本地页面，是"窗口"不是"数据源"——它读工作区和各 Application 的状态并展示，从不自己存储真相。

**建议包含的模块**（不必一次做全，从 3 个开始）：

- 最近产出的 artifacts 列表（一键打开）
- 高频 Skill 的执行按钮（headless 触发）
- Routine 运行状态板（读 `runs.log`）
- 需要人工确认的待办（高风险动作排队等待你点头的地方）

**技术要求**：固定本地地址、进程守护保证重启后仍可访问、单页面自包含、逐步添加模块（每加一个模块是一次对话，不是一个项目）。

---

## 9. 治理与运维

- **每周 15 分钟维护**：清理过期路由指针、检查 Routine 是否真的按时产出、把这周重复问了两次的问题封装成新 Skill。
- **模型/effort 分级**：清理类、摘要类任务用轻量模型；涉及复杂判断、对外发布前的最终检查用更强模型。这直接决定长期运行成本。
- **单人 vs 团队扩展**：本规范默认单用户场景，Memory/Skill 都是个人上下文。如果未来要多人共享，需要额外设计权限边界（谁能新增/修改哪些 Skill、Memory 里哪些内容对谁可见），不能直接把个人版扩大规模。

---

## 10. 落地路线图

沿用"自底向上、一天一层"的节奏，但按本规范的修正版执行：

| 阶段 | 内容 |
|---|---|
| 第 1-2 天 | 挑 1-2 个最高频的重复任务，写成 Skill（含护栏字段），跑通对话触发 |
| 第 3 天 | 建立 `CLAUDE.md` 主路由 + 1-2 个领域索引 |
| 第 4 天 | 把第 1 天的 Skill 接上 headless 命令行触发，验证可从"按钮"发起 |
| 第 5 天 | 梳理已有 Application/Connector，补齐 1 个最高价值的官方 MCP/API 接入 |
| 第 6 天 | 搭建指挥中心 v1（3 个模块：产出列表、Skill 按钮、待办） |
| 第 7 天 | 建立第一个 Routine（本地定时），跑通"电脑开着、任务自动执行"的完整链路 |
| 第 2 周起 | 引入 `runs.log`、`SKILLS_INDEX.md`，视需要评估是否上云端常驻机器 |

---

## 11. 上线前自检清单

- [ ] 每个 Skill 都写明了"绝对不能做的事"和"需要人工确认的动作"
- [ ] 新建 Skill 前查过 `SKILLS_INDEX.md`，无触发词冲突
- [ ] 路由文件（`CLAUDE.md` / `areas/*.md`）与实际文件位置一致，没有过期指针
- [ ] 至少一个 Routine 已用平台的持久化调度工具（而非会话内临时机制）配置并验证过真实触发
- [ ] 涉及外部平台写操作的 Skill，已确认走的是官方 MCP/API，而非脆弱的网页模拟
- [ ] `runs.log` 能看到最近一次定时任务的成功/失败记录
- [ ] 指挥中心能一页展示：最近产出、可执行 Skill、待你确认的事项
