# Electron 43 起，electron 包不再自带 postinstall，二进制要靠根项目的
# `postinstall: install-electron` 触发下载。默认源在部分网络下会失败
# （undici ETIMEDOUT），所以固定走镜像；在能直连 GitHub 的网络下删掉即可。
electron_mirror=https://npmmirror.com/mirrors/electron/
